# PostgreSQL Database URL (apenas em produção no Render)
# Copie da variável de ambiente DATABASE_URL do Render
# DATABASE_URL=postgresql://user:password@host:5432/database

# Se não definir DATABASE_URL, o app usa SQLite local (desenvolvimento)
